import { Shader } from "@/shaders/Shader";

export class VertexShader extends Shader {
  protected compileSource(gl: WebGL2RenderingContext | WebGLRenderingContext): void {
    // Vertex shader compilation logic
  }
}
